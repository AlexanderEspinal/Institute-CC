{
  "name": "institute-cc",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"pnpm --filter institute-cc-server dev\" \"pnpm --filter client dev\"",
    "dev:server": "pnpm --filter institute-cc-server dev",
    "dev:client": "pnpm --filter client dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
